<% if(page == "/login"){%>
    <% include login %>
<%}%>

<% if(page == "/join"){%>
    <% include join %>
<%}%>

<% if(page == "/reset-link"){%>
    <% include reset-link %>
<%}%>

<% if(page == "/reset-password"){%>
    <% include reset-password %>
<%}%>

<% if(page == "/profile"){%>
    <% include profile %>
<%}%>